<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hxce.api.dao.PermissionDao">
    <select id="searchUserPermissions" resultType="String" parameterType="Long">
        SELECT p.permission_code AS permissionCode
        FROM tb_user u
                 JOIN tb_role r ON JSON_CONTAINS(u.roles, CAST(r.id AS CHAR))
                 JOIN tb_permission p ON JSON_CONTAINS(r.permissions, CAST(p.id AS CHAR))
        WHERE u.id = #{userId}
    </select>
    <select id="searchAllPermission" resultType="HashMap">
        SELECT p.id, 
               m.module_name AS moduleName, 
               p.permission_name AS permissionName, 
               p.permission_code AS permissionCode
        FROM tb_permission p
        LEFT JOIN tb_module m ON p.module_id = m.id
        ORDER BY m.module_name, p.id
    </select>
</mapper>